{% extends "base.twig" %}

{% block hero %}
  {{ fn('depicter', 1) }}
  {# <div
    class="bg-center bg-no-repeat bg-cover h-96"
    style="background-image: url({{ hero_image.src }})"
  >
    <div class="flex flex-col items-center justify-center w-full h-full gap-6 bg-gray-700 bg-opacity-20">
      <p class="text-4xl text-white font-prata md:text-6xl">{{ hero_txt }}</p>
      <a href="{{ hero_link.link }}" class="button">{{ hero_btn_txt }}</a>
    </div>
  </div> #}
{% endblock %}

{% block content %}
  {{ post.content }}
{% endblock %}